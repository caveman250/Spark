SSL_FRAGMENT

port(FragmentUV) in vec2 inUV;
port(FragColor) out vec4 color;

uniform sampler2D Texture;

void main()
{
    float opacity = texture(Texture, inUV).r;
    color = vec4(1, 1, 1, opacity);
}