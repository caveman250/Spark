if (${PLATFORM} MATCHES Windows)
    include(windows/WindowsPlatform)
elseif (${PLATFORM} MATCHES Linux)
    include(linux/LinuxPlatform)
elseif (${PLATFORM} MATCHES Mac)
    include(mac/MacPlatform)
endif()