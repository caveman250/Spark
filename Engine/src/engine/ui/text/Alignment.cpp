#include "Alignment.h"

DEFINE_SPARK_ENUM_BEGIN(se::ui::text::Alignment)
    DEFINE_ENUM_VALUE(Alignment, Left)
    DEFINE_ENUM_VALUE(Alignment, Center)
    DEFINE_ENUM_VALUE(Alignment, Right)
DEFINE_SPARK_ENUM_END()