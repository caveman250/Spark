#include "spark.h"
#include "ImageComponent.h"

namespace se::ui::components
{
    DEFINE_SPARK_COMPONENT_BEGIN(ImageComponent) // TODO serialisation + asset references + materials as assets
    DEFINE_SPARK_COMPONENT_END(ImageComponent)
}