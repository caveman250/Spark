#include "spark.h"
#include "TextComponent.h"

namespace se::ui::components
{
    DEFINE_SPARK_COMPONENT_BEGIN(TextComponent) // TODO serialisation + asset references + materials as assets
    DEFINE_SPARK_COMPONENT_END(TextComponent)
}