#include "Anchors.h"

namespace se::ui
{
    DEFINE_SPARK_POD_CLASS_BEGIN(Anchors)
        DEFINE_MEMBER(left)
        DEFINE_MEMBER(right)
        DEFINE_MEMBER(top)
        DEFINE_MEMBER(bottom)
    DEFINE_SPARK_CLASS_END()
}